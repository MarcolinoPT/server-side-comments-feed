# server-side-comments-feed

A very simple example of server sider comments feed.
The Docker compose is not working but you can run it to start the database container and then use the dockerized project.
